
import React from 'react';

interface ReferenceButtonsProps {
  pngUrl: string;
  htmlUrl?: string;
}

const ReferenceButtons: React.FC<ReferenceButtonsProps> = () => {
  return null;
};

export default ReferenceButtons;
